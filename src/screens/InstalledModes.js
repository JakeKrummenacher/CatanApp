import React from "react";
import { Text, View } from "react-native";

export default function InstalledModes({AppState, navigation}){

    return(
        <View>
            <Text>placement for installed modes</Text>
        </View>
    )
}